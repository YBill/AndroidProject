<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bill.androidproject">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dagger2.Dagger2Activity" />
        <activity android:name=".databinding.DataBindingActivity" />
        <activity android:name=".vp2.ViewPager2Activity" />
        <activity android:name=".fragment_sycn_fragment.FragmentSycnActivity" />
        <activity android:name=".lv.LVTestActivity" />
        <activity android:name=".paging.PagingTestActivity" />
        <activity android:name=".room.RoomTestActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>